# Environment Variables Template
# Copy this file to .env.development and .env.production and fill in your values.
#
# Auth is handled entirely by the PantherWatch backend (Google OAuth + our own JWT),
# so the frontend no longer needs any Supabase keys. The API base URL is configured
# in src/config/apiConfig.js. Add any future client-side env vars below.
